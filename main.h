#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

int myprint(char *str);
int _strlen(char *s);
char *_strcpy(char *dest, char *src);

#endif
